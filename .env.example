# Balkanza read-only database connection string.
# Copy this file to .env.local for local development, and set the same
# variable in your Vercel project settings (Settings -> Environment Variables).
#
# Use the READ-ONLY user. Never commit real credentials.
DATABASE_URL="postgresql://readonly_user:PASSWORD@ep-sweet-salad-aee19c0f-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require"
